import { ElementRef, EventEmitter } from "@angular/core";
import { NgSelectPluginGeneric } from "../../../misc";
import { NgSelectPluginInstances } from "../../../components/select";
import { NoTextsLocatorOptions, NoTextsLocator } from "./noTextsLocator.interface";
/**
 * Component used for rendering no texts locator
 */
export declare class NoTextsLocatorComponent implements NoTextsLocator, NgSelectPluginGeneric<NoTextsLocatorOptions> {
    ngSelectPlugins: NgSelectPluginInstances;
    pluginElement: ElementRef;
    /**
     * Options for NgSelect plugin
     */
    options: NoTextsLocatorOptions;
    /**
     * Indication that texts should be obtained again, because they have changed
     */
    textsChange: EventEmitter<void>;
    constructor(ngSelectPlugins: NgSelectPluginInstances, pluginElement: ElementRef);
    /**
     * Gets text for specified key
     */
    getText(key: string): string;
    /**
     * Initialize plugin, to be ready to use, initialize communication with other plugins
     */
    initialize(): void;
    /**
     * Initialize plugin options, all operations required to be done with plugin options are handled here
     */
    initOptions(): void;
    /**
     * Explicitly runs invalidation of content (change detection)
     */
    invalidateVisuals(): void;
}
