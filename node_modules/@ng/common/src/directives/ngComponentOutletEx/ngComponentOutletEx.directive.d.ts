import { EventEmitter, Injector, NgModuleFactory, OnChanges, OnDestroy, SimpleChanges, Type, ViewContainerRef } from '@angular/core';
/**
 * Instantiates a single {@link Component} type and inserts its Host View into current View.
 */
export declare class NgComponentOutletEx<TComponent> implements OnChanges, OnDestroy {
    private _viewContainerRef;
    /**
     * Created component reference
     */
    private _componentRef;
    /**
     * Created custom module reference
     */
    private _moduleRef;
    /**
     * Type that should be dynamically created into current container
     */
    ngComponentOutletEx: Type<TComponent>;
    /**
     * Custom injector that will be used for newly created component
     */
    ngComponentOutletExInjector: Injector;
    /**
     * Projectable nodes that can be injected into component
     */
    ngComponentOutletExContent: any[][];
    /**
     * Different module factory that is used for creation of new component
     */
    ngComponentOutletExNgModuleFactory: NgModuleFactory<any>;
    /**
     * Occurs when component is created or destroyed, it can send instance of component, or null
     */
    ngComponentOutletExCreated: EventEmitter<TComponent | null>;
    /**
     * Instance of dynamically created component
     */
    readonly component: TComponent | null;
    constructor(_viewContainerRef: ViewContainerRef);
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
}
